version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: ddd_inventory_app
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env
    volumes:
      - ./:/var/www/html
    networks:
      - ddd-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - db

  db:
    image: timescale/timescaledb:latest-pg15
    container_name: ddd_inventory_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USERNAME:-ddd_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
      POSTGRES_DB: ${DB_DATABASE:-ddd_inventory}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "5436:5432"
    networks:
      - ddd-network

  web:
    image: nginx:alpine
    container_name: ddd_inventory_web
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - ddd-network
    depends_on:
      - app
      - db

  db-backup:
    image: prodrigestivill/postgres-backup-local:15-alpine
    container_name: ddd_inventory_backup
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=${DB_DATABASE:-ddd_inventory}
      - POSTGRES_USER=${DB_USERNAME:-ddd_user}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-secret}
      - BACKUP_NUM_KEEP=7
      - BACKUP_FREQUENCY=daily
      - SCHEDULE=@daily
    volumes:
      - ./infra/db/backups:/backups
    networks:
      - ddd-network
    depends_on:
      - db

  outbox-worker:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: ddd_inventory_outbox_worker
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env
    volumes:
      - ./:/var/www/html
    networks:
      - ddd-network
    command: ["php", "scripts/outbox-worker.php"]
    depends_on:
      - db

  queue-worker:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: ddd_inventory_queue_worker
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env
    volumes:
      - ./:/var/www/html
    networks:
      - ddd-network
    command: ["php", "scripts/queue-worker.php"]
    depends_on:
      - db

networks:
  ddd-network:
    driver: bridge

volumes:
  db_data:
